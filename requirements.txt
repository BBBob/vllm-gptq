ninja == 1.11.1.1 # For faster builds.
psutil == 5.9.8
ray == 2.9.3
pandas == 2.2.0 # Required for Ray data.
pyarrow == 15.0.0  # Required for Ray data.
sentencepiece == 0.1.99  # Required for LLaMA tokenizer.
numpy == 1.26.4
einops == 0.7.0 # Required for phi-1_5
torch == 2.1.2
transformers == 4.37.1  # Required for Mistral.
xformers == 0.0.23.post1  # Required for CUDA 12.1.
fastapi == 0.110.0
uvicorn[standard] == 0.27.0
pydantic == 1.10.13  # Required for OpenAI server.
auto-gptq == 0.6.0
